version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"  # Python version you want to use

sphinx:
  configuration: docs/conf.py  # Path to your Sphinx configuration file

python:
  install:
    - requirements: docs/requirements.txt  # Ensure all dependencies are listed here
