version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"  # Python version to use

sphinx:
  configuration: docs/conf.py  # Path to Sphinx configuration

python:
  install:
    - requirements: requirements.txt  # Pointing to the root-level requirements.txt
